# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*
wrangler.toml

node_modules
dist
dist-ssr
*.local

# Environment and credentials (R2 migration, deploy tokens): never versioned.
.env
.env.*
!.env.example
!.env.production

# Netlify CLI local state (site link), never versioned.
.netlify/
.freebuff/

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Vice City game data (served outside Git: Desktop via vcWeb in dev, R2/host in deploy)
public/game/
*.wasm
*.data

# Trailer served outside Git (public/game in dev, R2 in deploy)
src/assets/trailers/*.mp4