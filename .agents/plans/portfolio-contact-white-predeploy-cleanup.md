---
name: portfolio-contact-white-predeploy-cleanup
status: EXECUTED
type: maintenance
domain: portfolio
owner_rules: .agents
created: 2026-09-12 12:00
enriched: 2026-09-12
ready: 2026-09-12 (revision orquestador: aprueba alcance, fix invert, excepcion GlitchCursor:202; orden "ejecuta el plan")
executed: 2026-09-12
---

# Plan: contacto blanco dual-theme + limpieza comentarios + consola 5173 + aliste despliegue

> Hechos decididos por el usuario (no son puntos abiertos):
> - Credito `GradualBlur.vue:1-13` ("GradualBlur de vue-bits por David Haz") se ELIMINA: el usuario confirma autoria propia (divs con blurs, mas simple, no es el de David Haz).
> - Unico credito que se conserva: `FaultyTerminalBackground.vue:1-7` ("Fondo animado basado en FaultyTerminal de vue-bits por David Haz").
> - Permiso de scripts OTORGADO ("tienes permiso para ejecutar lo que necesites"): Executor podra ejecutar `bun run dev`, `bun run lint:check`, `bun run format`, `bun run lint`, `bun run build`. Sigue prohibido Git y borrar archivos (RULES 0.1/0.2).
> - Este plan absorbe el pendiente de build del plan anterior `portfolio-khatarsis-image-contact-projects-about` (EXECUTED con `bun run build` pendiente por regla 0.4).

## Plan Tecnico

### Analisis

- Objetivo:
  - (1) Logos de contacto blancos en ambos themes (dark y light).
  - (2) Cero comentarios en `src/**` salvo el credito FaultyTerminal (`FaultyTerminalBackground.vue:1-7`).
  - (3) Consola del dev server (`:5173`, navegador + terminal) sin errores/warnings; corregir lo que aparezca.
  - (4) Repo limpio predeploy: `lint:check`/`format:check` -> `format`/`lint --fix` (permiso otorgado) -> `bun run build` (`vue-tsc -b && vite build`).
- Scope:
  - Incluido: `ContactImages.vue` (tinta blanca); inventario+eliminacion de comentarios en `src/**` (`.vue`, `.ts`, `.css`); `bun run dev` + revision consola `:5173` + correcciones; `lint:check`/`format:check` luego `format`/`lint` con fix luego `bun run build`.
  - Fuera de alcance: cambios visuales/funcionales no pedidos, tocar `khatarsis` (`file:../khatarsis`), tocar CV (`../cv`), borrar archivos, Git, `preview` salvo necesidad (no se preve).
- Archivos:
  - Contacto: `src/components/contact/ContactImages.vue`, `src/components/contact/ContactSection.vue` (solo lectura salvo que el fix lo exija), `src/components/common/SocialLinks.vue` (lectura/auditoria, sin cambio funcional), `src/assets/brands/{github,linkedin,whatsapp,mail}.svg` (lectura verificada), `src/data/portfolio.ts`, `src/i18n/locales/es.ts`, `src/i18n/locales/en.ts` (lectura/auditoria divergencias).
  - Comentarios (31 ficheros con hits verificados por `rg`, conteos abajo): `src/App.vue`, `src/views/MainView.vue`, `src/router/index.ts`, `src/i18n/index.ts`, `src/khatarsis.d.ts`, `src/data/portfolio.ts`, `src/i18n/locales/es.ts`, `src/i18n/locales/en.ts`, `src/composables/{useTheme,useStaticFlash,useCrtIntro,useReveal,useSectionSpy,useSmoothScroll}.ts`, `src/components/about/MapCard.vue`, `src/components/about/AboutSection.vue`, `src/components/projects/ProjectCard.vue`, `src/components/projects/ProjectsSection.vue`, `src/components/layout/FooterSection.vue`, `src/components/layout/GradualBlur.vue`, `src/components/background/{FaultyTerminalBackground,TvStaticBackground}.vue`, `src/components/crt/Crt.vue`, `src/components/hero/GlitchText.vue`, `src/components/cursor/GlitchCursor.vue`, `src/components/contact/ContactImages.vue`, `src/components/common/SocialLinks.vue`, `src/style.css`. Sin comentarios (verificado, no-op): `src/components/hero/HeroSection.vue`, `src/components/layout/Navbar.vue`, `src/main.ts`.
  - Planes existentes en `.agents/plans/`: 30 entradas revisadas; el unico antecedente es `portfolio-khatarsis-image-contact-projects-about` (EXECUTED, build pendiente absorbido aqui); no duplicar.
- Divergencias detectadas por lectura vs lo ejecutado (usuario hizo cambios fuera de planes):
  - `ContactImages.vue` actual NO usa SVGs inline: importa 4 SVG locales como URLs (`github/linkedin/whatsapp/mail.svg`, existen verificados) y los renderiza via `k-image` (= `<img>`) dentro de `k-card as="a"` con `v-effect spotlight size 77` + wrapper `div v-effect skew` + `backdrop-blur-sm`. El plan PENDING describia "SVGs con `fill=currentColor`, forzar blanco del wrapper con `text-white`" — eso es insuficiente tal cual (ver Riesgos).
  - `ContactSection.vue` actual envuelve `<contact-images />` en `div.flex.flex-1.gap-4[data-reveal]`; `SocialLinks.vue` conserva `k-button` hero intacto (sin drift funcional).
  - `portfolio.ts` / `es.ts` / `en.ts` coinciden con lo ejecutado (orden `businessSuite, softii, brilla, khatarsis, alytos`; `Vite` en frontend, `tools: ['Git','IA']`; `about.subtitle` nuevo + `about.body` reescrito ES+EN; `social.ariaEmail` presente). Sin drift de datos/copy.
  - `src/assets/brands/` ahora contiene los 4 SVG de contacto (la decision Opcion A del plan anterior quedo materializada).
- Riesgos:
  - Tinta: los 4 brand SVG tienen `fill="currentColor"` pero se sirven como `<img src>` via `k-image`; el documento SVG externo NO hereda `currentColor` del wrapper, por defecto pinta negro en ambos themes (coincide con el reporte "negras en modo oscuro"). Solo `text-white` en el `<a>`/`k-card` NO las blanquea. Fix correcto Tailwind-first: anadir filtro Tailwind (`invert`, y `text-white` en el wrapper como base) sobre el `k-image`, sin CSS nativo ni arbitrarios. No tocar los `.svg` ni `khatarsis`.
  - Comentarios dentro de strings GLSL (`FaultyTerminalBackground.vue` lineas 111/194/243-245, `TvStaticBackground.vue` lineas 86/93-94/120/125-126/128/133/139/142/146): son parte de template literals de shaders; el edit debe quitar solo la linea de comentario sin romper el shader; `bun run build` + revision visual de fondos lo cubre.
  - `GlitchCursor.vue:202` (`<!-- Pointing-hand silhouette from Google Material Symbols touch_app (Apache License 2.0): ... -->`) es atribucion de licencia: NO borrar a ciegas; se reporta y queda pendiente de confirmacion explicita (riesgo legal). Es la unica excepcion al "borrar todo" ademas del credito FaultyTerminal.
  - Ningun `<!-- -->` revisado tiene efecto layout real (el layout viene de clases Tailwind); aun asi Executor verifica visualmente tras el borrado (contacto, mapa, proyectos, footer, bandas blur, fondos).
  - Consola `:5173`: pueden salir warnings de a11y/GSAP/Leaflet/Lenis/i18n; corregir solo dentro del scope (no refactors no pedidos).

### Cambios

- `src/components/contact/ContactImages.vue` (edit): tinta blanca dual-theme. Anadir `text-white` a cada `k-card as="a"` (base de tinta + a11y sin cambio) y `invert` al `class` de cada `k-image` (convierte el negro `currentColor`-por-defecto en blanco en ambos themes). Opcional robusto `brightness-0 invert` si algun asset trajera color residual; ambas son utilidades estandar, sin `[...]`, sin CSS nativo. Preservar `v-effect skew` en el `div` exterior, `spotlight` en `k-card`, `data-reveal` en el padre (`ContactSection.vue`, no mover), `:href`/`mailto:`/`target`/`rel`/`aria-label`/`alt`/`fit="contain"`/`hover="none"`/`lazy`/`h-16 w-16 sm:h-20 sm:w-20`. Eliminar sus comentarios (lineas 2-6, 16-17) en el mismo edit.
- `src/components/layout/GradualBlur.vue` (edit): eliminar bloque credito lineas 1-13 (decision usuario: autoria propia) + resto de comentarios (31-33, 63-65, 67-69, 89-90, 105-106). No tocar logica `layers`/`containerClass`/`containerStyle` ni template funcional.
- `src/components/background/TvStaticBackground.vue` (edit): eliminar cabecera 1-11 + comentarios TS/GLSL (86, 93-94, 120, 125-126, 128, 133, 139, 142, 146, 208-209, 218, 224, 265) + comentario template 373-374. No tocar uniforms, loop, `watch(theme)`, directivas.
- `src/components/background/FaultyTerminalBackground.vue` (edit parcial): NO TOCAR lineas 1-7 (credito). Eliminar resto (111, 194, 243-245, 304-305, 338, 415, 461-462, 504). No tocar shaders funcionales ni ciclo RAF.
- `src/components/cursor/GlitchCursor.vue` (edit parcial): eliminar todos los comentarios EXCEPTO linea 202 (licencia Material Symbols `touch_app` Apache-2.0) que se REPORTA y se deja intacta pendiente de confirmacion. Incluye bloques 6-10, 13, 15, 17, 22, 24, 29-30, 34-37, 68, 72, 88, 95-96, 122, 129 y CSS 236, 256, 338.
- `src/style.css` (edit parcial): eliminar todos los bloques `/* */` (1-5, 9-15, 36-38, 43-44, 50-53, 66-67, 71-72, 81-88, 93-99, 115, 130, 139, 145-147, 152-153, 177, 184-185, 187-189, 219-222, 240-242, 273-274). NO TOCAR directivas de herramienta (no son comentarios): `@import 'khatarsis/style.css'`, `@import 'tailwindcss'`, `@custom-variant`, `@theme`, `@supports not selector(...)`, ni los `background-image: url("data:image/svg+xml;...")` (281-310). No tocar ninguna regla CSS, solo sus comentarios.
- Resto `src/**` (edit limpieza, sin cambio funcional): `src/App.vue` (44); `src/views/MainView.vue` (19-20, 24-26, 36-37); `src/router/index.ts` (6-8, 11, 42-43); `src/i18n/index.ts` (5-6); `src/khatarsis.d.ts` (1-3, 50-53); `src/data/portfolio.ts` (1-6, 40-41, 95-96, 128-130); `src/i18n/locales/es.ts` (1-2); `src/i18n/locales/en.ts` (1-2); `src/composables/useTheme.ts` (16); `src/composables/useStaticFlash.ts` (18); `src/composables/useCrtIntro.ts` (3-4); `src/composables/useReveal.ts` (1-3); `src/composables/useSectionSpy.ts` (1-10, 20, 26, 42-43, 68); `src/composables/useSmoothScroll.ts` (1-3, 35); `src/components/about/MapCard.vue` (todos menos el string `PIN_SVG` de linea 31 que es codigo, no comentario; incluye template 164-165); `src/components/about/AboutSection.vue` (13-14, 51-52); `src/components/projects/ProjectCard.vue` (22-23); `src/components/projects/ProjectsSection.vue` (28-30); `src/components/layout/FooterSection.vue` (19-23, 37-38); `src/components/crt/Crt.vue` (43-44); `src/components/hero/GlitchText.vue` (14, 16, 45, 293); `src/components/common/SocialLinks.vue` (2-5). Verificado: `HeroSection.vue`, `Navbar.vue`, `main.ts` sin comentarios (no-op). Verificado: cero `eslint-disable` / `@ts-*` / `/// <reference` en `src` (nada que preservar por herramientas).

### Restricciones

- RULES 0.1/0.2: no Git, no borrar archivos. Los comentarios se eliminan por pedido explicito; si un comentario pareciera load-bearing al editar, reportar y no borrar a ciegas (aplica a `GlitchCursor.vue:202` que queda intacto).
- RULES 0.3: un componente principal por requerimiento; no crear subcomponentes/helpers nuevos.
- RULES 0.4: scripts solo los autorizados (`bun run dev`, `bun run lint:check`, `bun run format`, `bun run lint`, `bun run build`); `preview` no previsto. Scripts reales base en `.agents/AGENTS.md` + extras verificados en `package.json` (`lint`, `lint:check`, `format`, `format:check`).
- RULES 0.13 + CODING 2.1: lo que quede debe seguir English-only (los comentarios espanoles de `useReveal.ts`/`useSmoothScroll.ts`/`TvStaticBackground.vue:86/93-94/265` desaparecen con la limpieza; no introducir spanglish ni `handle*` nuevos).
- CODING 7.1: cero margenes (`m-*` prohibidos); el fix usa clases existentes + `text-white`/`invert`, sin margenes nuevos.
- CODING 7.2/7.3/7.4/7.5: no fijar `width/height` con flex, no `min-w-0` por defecto, no tocar tipografia, sin arbitrarios `[...]` (el fix usa escala estandar `invert`, `text-white`, `h-16 w-16 sm:h-20 sm:w-20` ya existentes).
- CODING 7.6: Tailwind-first. El fix de tinta se resuelve con utilidades; CSS nativo solo quedaria justificado si `invert` no blanqueara (documentar en cierre si ocurriera; no previsto).
- Accesibilidad intacta: no tocar `aria-label` (`t('social.aria*')`), `alt` (`GitHub/LinkedIn/WhatsApp/Email`), `role="img"` de los SVG, foco/contraste.
- DESIGN.md: respetar capas (data en `portfolio.ts`, copy en locales, presentacion en componentes); sin endpoints, sin secretos (`contact.email` es dato publico del portfolio).

### Steps

1. Archivo `src/components/contact/ContactImages.vue` + lecturas `src/components/contact/ContactSection.vue`, `src/components/common/SocialLinks.vue`, `src/data/portfolio.ts`, `src/i18n/locales/es.ts`, `src/i18n/locales/en.ts` — Accion `read` (verificar divergencias) + `edit` solo en `ContactImages.vue`. Detalle: confirmar estructura actual (`div v-effect skew` > `k-card as="a" spotlight` > `k-image img`); aplicar tinta dual-theme: anadir `text-white` al `class` de cada `k-card` y `invert` al `class` de cada `k-image` (4 externos + 1 mail), preservando `h-16 w-16 sm:h-20 sm:w-20`, `fit/hover/lazy`, `href/mailto:/target/rel/aria-label/alt`; en el mismo edit eliminar comentarios lineas 2-6 y 16-17. Restricciones: English-only lo que quede; Tailwind cero-margenes/sin arbitrarios/Tailwind-first; a11y intacta; sin Git/borrados. Verificacion esperada: `read` posterior muestra `text-white` en los 4 `k-card` e `invert` en los 4 `k-image`, cero comentarios en el archivo, resto de props intacto.
2. Archivos `src/components/layout/GradualBlur.vue`, `src/components/background/TvStaticBackground.vue`, `src/components/background/FaultyTerminalBackground.vue` — Accion `read` + `edit`. Detalle: `GradualBlur.vue` eliminar 1-13 + 31-33 + 63-65 + 67-69 + 89-90 + 105-106; `TvStaticBackground.vue` eliminar 1-11 + 86 + 93-94 + 120 + 125-126 + 128 + 133 + 139 + 142 + 146 + 208-209 + 218 + 224 + 265 + 373-374 (en GLSL quitar solo la linea de comentario dentro del literal, sin alterar codigo shader); `FaultyTerminalBackground.vue` NO TOCAR 1-7, eliminar 111 + 194 + 243-245 + 304-305 + 338 + 415 + 461-462 + 504. Restricciones: sin cambios funcionales; English-only; sin Git/borrados; no tocar directivas. Verificacion esperada: `rg` en los 3 archivos solo devuelve el bloque `FaultyTerminalBackground.vue:1-7`; fondos siguen compilando (se confirma en Step 6 con build).
3. Archivos `src/components/cursor/GlitchCursor.vue` (parcial), `src/components/about/MapCard.vue`, `src/components/about/AboutSection.vue`, `src/components/projects/ProjectCard.vue`, `src/components/projects/ProjectsSection.vue`, `src/components/layout/FooterSection.vue`, `src/components/crt/Crt.vue`, `src/components/hero/GlitchText.vue` — Accion `read` + `edit`. Detalle: eliminar todos los comentarios listados en `### Cambios` salvo `GlitchCursor.vue:202` (licencia Apache-2.0 `touch_app`) que se deja intacto y se reporta; en `MapCard.vue` no tocar el string `PIN_SVG` (codigo); en `GlitchText.vue:293` quitar solo el comentario CSS, no la regla. Restricciones: a11y intacta (`aria-hidden`, labels); cero-margenes/sin arbitrarios; sin Git/borrados. Verificacion esperada: `rg` en estos archivos devuelve unicamente `GlitchCursor.vue:202`; templates y clases sin otro cambio.
4. Archivos `src/App.vue`, `src/views/MainView.vue`, `src/router/index.ts`, `src/i18n/index.ts`, `src/khatarsis.d.ts`, `src/data/portfolio.ts`, `src/i18n/locales/es.ts`, `src/i18n/locales/en.ts`, `src/composables/useTheme.ts`, `src/composables/useStaticFlash.ts`, `src/composables/useCrtIntro.ts`, `src/composables/useReveal.ts`, `src/composables/useSectionSpy.ts`, `src/composables/useSmoothScroll.ts`, `src/components/common/SocialLinks.vue` — Accion `read` + `edit` limpieza. Detalle: eliminar exactamente los rangos de `### Cambios`; no tocar imports, tipos, logica, copy visible ni paridad es/en. Restricciones: English-only; sin cambios funcionales; sin Git/borrados. Verificacion esperada: `rg "<!--|// |/\*"` en estos archivos = 0 hits.
5. Archivo `src/style.css` — Accion `read` + `edit` solo-comentarios. Detalle: eliminar los bloques `/* */` listados en `### Cambios`; dejar intactos `@import` x2, `@custom-variant`, `@theme`, `@supports`, todas las reglas y los `background-image: url("data:image/svg+xml;...")`. Restricciones: Tailwind-first; sin arbitrarios nuevos; sin Git/borrados. Verificacion esperada: `rg "/\*"` en `style.css` = 0; reglas y directivas intactas por `read`.
6. Consola `:5173` — Archivos segun lo que la consola pida (dentro del scope) — Accion `read` + `edit` correctivo + scripts autorizados. Detalle: levantar `bun run dev`, revisar terminal + consola del navegador (errores y warnings: a11y, i18n keys, GSAP/ScrollTrigger, Leaflet, Lenis, shaders), corregir solo lo necesario dentro del scope (tinta, comentarios que rompieran algo, errores reales); incluir `vue-tsc` via `bun run build` como verificacion final. Restricciones: solo scripts con permiso otorgado; sin Git/borrados; sin ampliar scope a refactors no pedidos. Verificacion esperada: terminal sin errores, consola navegador sin errores/warnings (o warnings residuales de terceros documentados), contacto blanco en light+dark, fondos/blur/mapa/reveal intactos.
7. Predeploy lint/format/build — Sin archivo (scripts autorizados). Detalle en orden: `bun run lint:check` + `bun run format:check` (lectura, anotar fallos), luego `bun run format` + `bun run lint` (con fix, permiso otorgado), re-leer diffs de formato para confirmar que solo tocan estilo/comentarios ya tratados, luego `bun run build` final (absorbe el pendiente del plan anterior). Restricciones: `bun` (no npm); sin Git; si `lint`/`format` proponen cambios fuera del scope, reportar antes de aceptar. Verificacion esperada: `lint:check` y `format:check` en verde tras el fix; `build` (`vue-tsc -b && vite build`) en verde.

### Verificacion

- Busqueda comentarios: `rg` en `src/**` (`*.vue,*.ts,*.css`) devuelve UNICAMENTE `FaultyTerminalBackground.vue:1-7` + `GlitchCursor.vue:202` (reportado pendiente); cero `eslint-disable`/`@ts-*` nuevos; directivas `@import/@theme/@custom-variant/@supports` intactas.
- Visual light+dark: 4 logos de contacto blancos en dark y en light; `alt`/`aria-label` intactos; skew/spotlight/reveal, fondos FaultyTerminal/TvStatic, gradual-blur, mapa, proyectos sin regresion.
- Consola `:5173` (navegador + terminal de `bun run dev`) sin errores; warnings residuales de terceros documentados o corregidos.
- `bun run lint:check`, `bun run format:check`, `bun run build` en verde (build absorbe el pendiente del plan `portfolio-khatarsis-image-contact-projects-about`).
- Validacion estandares: DESIGN.md (capas data/locales/componentes, sin endpoints/secretos nuevos), CODING_STANDARDS.md (English-only restante, flex+gap sin margenes, sin fijos con flex, sin arbitrarios, Tailwind-first con `text-white`/`invert`), RULES.md (sin Git/borrados, alcance acotado, scripts solo autorizados, cero duplicacion, skills subordinadas; `.agents/skills/README.md` sin skills registradas, baseline Vue 3 + Tailwind + Vite + TS + `bun` de `.agents/AGENTS.md`).

## Cierre (memoria persistente)

- Que cambio: tinta blanca dual-theme en contacto (`text-white` en 4 `k-card` + `invert` en 4 `k-image`); limpieza de comentarios en 31 ficheros `src/**` (solo sobreviven credito FaultyTerminal + licencia `GlitchCursor.vue:179`); credito falso de GradualBlur eliminado; `lint --fix` reordeno atributos en `Navbar.vue:46`/`FooterSection.vue:29` (estilo, sin impacto).
- Verificacion: `bun run lint:check` verde, `bun run format:check` verde, `bun run build` (`vue-tsc -b && vite build`) verde con `dist/` emitido; `rg <!--` solo devuelve credito FaultyTerminal + licencia GlitchCursor + 2 comentarios internos de assets SVG (`logo_khatarsis.svg:3`, `portrait.svg:2`, fuera de scope `.vue/.ts/.css`); `rg /\*` = 0; dev `:5173` ocupado por proceso del usuario, instancia propia en `:5174` sin errores + `curl` 200.
- Resultado: pendiente aceptacion visual del usuario (consola navegador + blanco light/dark + regresiones).
- Pendientes: revision visual en navegador por el usuario; `GlitchCursor.vue:179` (licencia Apache-2.0, requiere confirmacion explicita); warnings preexistentes fuera de scope (`__dirname` en `vite.config.ts`, chunk >500 kB); nit preexistente del usuario: `target="_blank"` en link `mailto:` (`ContactImages.vue:51`).
