---
name: portfolio-khatarsis-image-contact-projects-about
status: CLOSED
type: feature
domain: portfolio
owner_rules: .agents
created: 2026-09-12 12:00
enriched: 2026-09-12
ready: 2026-09-12 (orden "ejecuta el plan" tomada como visto bueno del ENRICHED + Opcion A logos locales)
executed: 2026-09-12
closed: 2026-09-12 (DoD: alcance conforme, build+lint verdes, aceptado "vale perfecto")
---

# Plan: imagenes khatarsis + contacto 4 logos + proyectos (orden/descripciones CV) + skills + about rewrite

> Solicitud del usuario (2026-09-12, con captura de Contacto): "necesito que todas las imagenes mostradas usen el componente image de khatarsis" / "para la seccion de contacto esos 3 botones pasaran a ser 4 images de khatarsis, con el efecto solo de skew, cada una mostrar como imagen el logo de medio por donde hacer contacto, si es github mostrar el logo de github, lo mismo con wpp y linkedin, añadiremos un extra que sera el logo de un correo, esa sera la de mi correo electronico que hara el tipico mailto: a mi correo" / "en las cards de proyectos cambia los textos por los textos que hay en mi cv como descripcion de cada proyecto por ejemplo para business suite deberia decir algo asi Business Suite 5.0 — Suite de servicios públicos para acueducto, gas, energía y aseo, con microservicios y microfrontends. Rol full stack con enfoque en frontend." / "tambien cambia el orden en como se muestran, el orden es primero business suite, despues softii, despues brilla, despues katharsis y por ultimo alytos" / "todos deben llevar esa mini descripcion que se encuentra en la plantilla de mi cv ya sabes donde buscarla por fuera de este repo en el repo de cv dentro de .agents" / "quita vite de herramientas ponlo en frontend y añade en herramientas IA" / "en el about me cambia el texto por algo asi (mejora gramatica... no literal)" + texto largo de Cesar Andres Rios + "cambia el 'Qué hago, cómo trabajo y de dónde vengo.' por 'Quien soy, en que he trabajado y de donde vengo'". Cierre del mensaje: "arma un plan de lo anterior".

## Plan Tecnico

### Analisis

- Objetivo:
  - (1) Todas las `<img>` visibles de la app renderizan con `k-image` de khatarsis (`layout="single"`), sin `<img>` nativo en `src/components/**`.
  - (2) Contacto pasa de 3 `k-button` a 4 `k-image` con solo efecto skew, cada una con logo del medio (GitHub, LinkedIn, WhatsApp, correo) y `mailto:` a `contact.email`.
  - (3) `projects` en orden `businessSuite, softii, brilla, khatarsis, alytos` con mini descripciones fieles al CV.
  - (4) Skills: `Vite` a `frontend`; `tools: ['Git', 'IA']`.
  - (5) Rewrite `about.body` ES+EN (gramatica mejorada, no literal) + `about.subtitle` nuevo.
- Scope:
  - Incluido estrictamente: migracion `<img>` -> `k-image`; bloque contacto 4 imagenes + `mailto:`; orden + descripciones proyectos; skills Vite/IA; rewrite about ES+EN + subtitulo.
  - Fuera de alcance: cambios visuales no pedidos (CRT, glitch, mapa, navbar, footer blur), nuevos endpoints, cambios en khatarsis, `bun run build/dev/preview` sin permiso (RULES 0.4).
- Archivos:
  - App: `src/components/common/SocialLinks.vue`, `src/components/contact/ContactSection.vue`, `src/components/hero/HeroSection.vue`, `src/components/projects/ProjectCard.vue`, `src/components/projects/ProjectsSection.vue` (solo lectura, itera `projects`), `src/components/about/AboutSection.vue`, `src/components/layout/Navbar.vue`, `src/components/layout/FooterSection.vue`, `src/data/portfolio.ts`, `src/i18n/locales/es.ts`, `src/i18n/locales/en.ts`, `src/main.ts` (solo lectura, confirma registro `k-*`), `src/khatarsis.d.ts` (solo lectura).
  - Khatarsis solo lectura: `C:/Users/s0rno/OneDrive/Documents/khatarsis/packages/khatarsis/src/components/miscellaneous/image/Image.vue`, `C:/Users/s0rno/OneDrive/Documents/khatarsis/packages/khatarsis/src/effects/skew.ts`.
  - CV solo lectura: `C:/Users/s0rno/OneDrive/Documents/cv/.agents/templates/cv/index.html`, `C:/Users/s0rno/OneDrive/Documents/cv/.agents/CONTEXT.md`.
  - Auditoria `<img>` verificada 2026-09-12 (4 coincidencias, unica fuente de verdad): `Navbar.vue:46` (logo), `FooterSection.vue:34` (logo), `AboutSection.vue:32` (portrait), `ProjectCard.vue:37` (project media). No hay `<img>` en `HeroSection.vue`, `ContactSection.vue`, `SocialLinks.vue` (usan `k-button` + `mdi:*`).
  - Planes existentes en `.agents/plans/`: 30 entradas revisadas; este es el unico sobre k-image/contacto/proyectos/about; no duplicar.
- Riesgos:
  - `SocialLinks.vue` compartido por hero y contacto (+ footer por comentario RULES 0.6). Cambiarlo in-place a 4 imagenes romperia hero. Decision desacoplar documentada abajo.
  - Fuente de los 4 logos no existe en repo. `src/assets/brands/` verificado contiene solo `softii-white-DuLy8ruX.svg`, `red-brilla-logo-JBY3JAAS.svg`, `logo_khatarsis.svg`, `arqbs-logo-color.png`. Prohibido inventar assets/binarios. Decision documentada abajo; Executor queda bloqueado en ese step sin aprobacion de origen/licencia.
  - `k-image` `hover` default es `scale` (verificado `Image.vue:433-438`, `496-504`, `1042-1044`). Pedido "solo skew" exige `hover="none"` explicito en contacto y en cards/portrait/logo para no competir con `v-effect="'skew'"` ni `spotlight`/reveal.
  - `v-effect="'skew'"` escribe `el.style.transform` (`skew.ts:69-72`). Nunca compartir elemento con `data-reveal`/GSAP/reveal ni con hover interno de `k-image`. Patron vigente `ProjectCard.vue:24-34` (skew en `div` exterior, spotlight en `k-card` interior) se replica en contacto: skew en `<a>` wrapper propio, `k-image` dentro con `hover="none"`.
  - `k-image` `rounded` prop equivale a `rounded-lg` (`Image.vue:1030-1032`); About actual usa `rounded-2xl`. No cambiar radio visual sin pedido: usar clase Tailwind, no prop, en portrait.
  - Copy visible solo en locales con paridad es/en; `about.body` es `string[]` consumido via `tm('about.body')` (`AboutSection.vue:15`); mantener tipo array.

### Cambios

- `src/data/portfolio.ts` (edit): reordenar `projects` a `businessSuite, softii, brilla, khatarsis, alytos` preservando `name/tech/url/image/lightInkLogo/descriptionKey`; mover `'Vite'` de `tools` a `frontend`; dejar `tools: ['Git', 'IA']`; extraer data social compartida para RULES 0.6 (ver Steps).
- `src/i18n/locales/es.ts` (edit): reemplazar `projects.items.businessSuite/softii/brilla/khatarsis/alytos.description` por minis del CV; anadir `social.ariaEmail`; reemplazar `about.subtitle` por `Quien soy, en que he trabajado y de donde vengo`; reescribir `about.body` (ES mejorado, no literal, con hechos del CV/borrador usuario).
- `src/i18n/locales/en.ts` (edit): paridad exacta de claves con `es.ts`; traducir las 5 descripciones, `social.ariaEmail`, `about.subtitle` -> `Who I am, what I have worked on and where I come from.`, y `about.body` EN espejo.
- `src/components/contact/ContactSection.vue` (edit): sustituir `<social-links />` por nuevo bloque de 4 imagenes; mantener layout `flex flex-wrap items-center justify-center gap-4`; mantener `data-reveal` en contenedor padre, nunca en el mismo `<a>` con skew.
- Nuevo `src/components/contact/ContactImages.vue` (create): bloque solo-contacto con 4 `<a v-effect="'skew'">` + `<k-image hover="none" fit="contain" lazy>`; enlaces `contact.githubUrl`, `contact.linkedinUrl`, `contact.whatsappUrl` (`target="_blank" rel="noopener"`) + `:href="'mailto:' + contact.email"`; `aria-label` via `t('social.ariaGitHub/.../ariaEmail')`; `alt` = label visible (`GitHub`, `LinkedIn`, `WhatsApp`, `Email`).
- `src/components/common/SocialLinks.vue` (edit minimo, solo extraccion data): hero conserva `k-button`; extraer array `links` a `src/data/portfolio.ts` (o `src/data/social.ts` si se justifica) para que ambos componentes importen misma fuente; no cambiar presentacion hero.
- `src/components/hero/HeroSection.vue` (read, sin edit salvo import si cambia fuente data): conserva `<social-links />` + `k-button` conocer; no aplicar skew ni k-image aqui.
- `src/components/projects/ProjectCard.vue` (edit): `<img v-if>` -> `<k-image v-if hover="none" fit="contain" lazy>` conservando `:src="props.project.image"`, `:alt="props.project.name"`, clase condicional `project-logo-light-ink`; no pasar `width/height` props; mantener wrappers skew/spotlight existentes sin tocar.
- `src/components/about/AboutSection.vue` (edit): `<img portrait>` -> `<k-image hover="none" fit="cover" lazy>` conservando `:src="portrait"`, `:alt="t('about.photoAlt')"`, `w-60 + rounded-2xl`; no usar prop `rounded` (evita cambio a `rounded-lg`); no pasar `width/height` props.
- `src/components/layout/Navbar.vue` (edit): `<img logo alt="">` -> `<k-image fit="contain" hover="none" :lazy="false">` con `class="h-8 w-auto"`; `alt=""` decorativo (boton ya tiene `aria-label t('nav.ariaHome')`).
- `src/components/layout/FooterSection.vue` (edit): igual que Navbar.
- Assets `src/assets/brands/github.svg, linkedin.svg, whatsapp.svg, mail.svg` (create pendiente de decision): solo si se aprueba Opcion A; no inventar contenido sin origen/licencia aprobados.

### Restricciones

- RULES 0.1/0.2: no Git, no borrar archivos; si un asset/componente parece obsoleto, reportar.
- RULES 0.3: un componente principal por requerimiento; no crear subcomponentes/modulos extra salvo necesidad tecnica explicada (el nuevo bloque contacto + extraccion data se justifican por desacoplo hero/contacto y se documentan aqui).
- RULES 0.4: no ejecutar `bun run dev/build/preview` (scripts reales en `.agents/AGENTS.md`: `bun run dev`, `bun run build` = `vue-tsc -b && vite build`, `bun run preview`); build queda pendiente con permiso explicito.
- RULES 0.6: cero duplicacion. Si se desacopla, extraer links sociales compartidos a `src/data/portfolio.ts` (o helper) e importar en ambos; no duplicar urls/labels/ariaKeys.
- RULES 0.13 + CODING 2.1: codigo 100% ingles (identificadores, props, comentarios, handlers `handle*`); copy visible solo en `es.ts/en.ts`; excepcion dominio (nombres propios/marcas) documentada en primer uso.
- CODING 7.1: cero margenes (`m-*/mt/mb/...` prohibidos); separacion con `flex + gap + padding`.
- CODING 7.2: si usa `grow/shrink/flex-*` no fijar `width/height`; en este plan no pasar `width/height` props a `k-image` en cards/portrait/logos (control por clases `h-48/h-8/w-60/max-h-full` del wrapper).
- CODING 7.3: `min-w-0` solo justificado; no anadir por defecto.
- CODING 7.4: no tocar `leading/tracking/text-*` como tamano salvo pedido.
- CODING 7.5: sin arbitrarios `[...]`; usar escala Tailwind. Excepcion ya existente `min-h-[70vh]` en secciones no se toca ni se replica.
- CODING 7.6: Tailwind-first con `@apply`; CSS nativo solo si imposible o pedido, documentado. No se preve CSS nativo nuevo.
- DESIGN.md: respetar capas (data en `portfolio.ts`, copy en locales, presentacion en componentes); no inventar reglas de stack; no exponer secretos; `contact.email` es dato publico del portfolio, no secreto.
- Khatarsis: `hover="none"` obligatorio donde pedido "solo skew" y donde ya existe skew/spotlight/reveal; `v-effect="'skew'"` en wrapper propio `<a>`/`<div>`, nunca en `k-image` ni en elemento con `data-reveal`/GSAP; `layout="single"` implicito (no usar `layout="grid"`); `lazy` true por defecto salvo logos navbar/footer (`:lazy="false"` por above-fold); `fit="contain"` para logos, `fit="cover"` para portrait.
- I18n: paridad total claves es/en; nada hardcodeado; `mailto:` construido desde `contact.email` (`cesarandresriosvalen@gmail.com` verificado en `portfolio.ts:80` y CV).
- Skills: `.agents/skills/README.md` sin skills registradas (tabla vacia); no se carga skill externa; se valida contra baseline `.agents/AGENTS.md` (Vue 3 Composition API + Tailwind + Vite + TS, `bun`, prefijo `k-*` via `file:../khatarsis/packages/khatarsis`). Ejemplos genericos de skills futuras se adaptarian a `bun` y scripts reales.

### Steps

1. Archivo `src/data/portfolio.ts` — Accion `read` + `edit`. Detalle: reordenar `export const projects: Project[]` a orden exacto: (1) `{ name: 'Business Suite', descriptionKey: 'businessSuite', tech: ['Vue 3','Spring Boot','Kubernetes','PL/SQL','Oracle','OCI'], url: 'https://ms-prd.saas.arqbs.com/login', image: arqbsLogo }`, (2) `{ name: 'Softii', descriptionKey: 'softii', tech: ['Vue','Quasar','Node.js/NestJS','MongoDB','AWS Lambda'], url: 'https://softii.business/login', image: softiiLogo, lightInkLogo: true }`, (3) `{ name: 'Brilla', descriptionKey: 'brilla', tech: ['Angular','Spring WebFlux','SQL Server','Azure DevOps','Kubernetes'], url: 'https://portal2.brilla.com.co/#/login/ally', image: brillaLogo }`, (4) `{ name: 'Khatarsis', descriptionKey: 'khatarsis', tech: ['Vue 3','TypeScript','Tailwind CSS'], url: 'https://github.com/s0rnero/khatarsis', image: khatarsisLogo }`, (5) `{ name: 'Alytos', descriptionKey: 'alytos', tech: ['Java','Spring Boot','WebFlux','R2DBC','Gradle'], url: 'https://github.com/s0rnero/alytos', image: '' }`; mover `'Vite'` a `frontend.skills` (lista final frontend: `['Vue.js','Angular','React','TypeScript','Quasar','Tailwind CSS','Vite','Microfrontends']`) y fijar `tools: ['Git','IA']`; extraer `export const socialLinks` o `getSocialLinks()` con `key/github/linkedin/whatsapp/email, href desde contact, icon mdi:*, label, ariaKey` para RULES 0.6. Restricciones: English-only identificadores/comentarios; no margenes; sin `[...]`; sin `width/height` fijos.
2. Archivo `src/i18n/locales/es.ts` — Accion `edit` (previo `read` ya realizado en enrichment). Detalle: `social.ariaEmail: 'Enviar correo a César Ríos'` (nueva clave); `projects.items.businessSuite.description: 'Business Suite 5.0 — Suite de servicios públicos para acueducto, gas, energía y aseo, con microservicios y microfrontends. Rol full stack con enfoque en frontend.'` (fuente `cv/.agents/templates/cv/index.html:123`); `projects.items.softii.description: 'Softii — Sistema de punto de venta para comercios en México, con arquitectura monolítica. Rol full stack con enfoque en frontend.'` (fuente `index.html:145`); `projects.items.brilla.description: 'Portal Brilla — Portal web de gestión para aliados del servicio, sobre microservicios. Rol full stack con enfoque en backend.'` (fuente `index.html:164`); `projects.items.khatarsis.description: 'Librería de componentes Vue y entorno para construir interfaces con contexto, velocidad y consistencia.'` (fuente `index.html:186`); `projects.items.alytos.description: 'Librería backend Java plug-n-play por módulos con auto-configuración: para armar la base y solo añadir la lógica de negocio.'` (fuente `index.html:196`); `about.subtitle: 'Quien soy, en que he trabajado y de donde vengo'` (literal pedido usuario); reescribir `about.body: string[]` en ES con gramatica mejorada incluyendo hechos obligatorios: nombre completo Cesar Andres Rios, 23 anos, Cali, 4+ anos, tres capas idea->produccion, Business Suite en empresas de servicios publicos mas grandes del pais, Portal Brilla con gaseras mas grandes, Khatarsis/Alytos, videojuegos/moto/perros Rocco y Rugal, cierre invitacion contacto; no hardcodear en componente. Restricciones: copy solo en locales; mantener `skills.areas.*` sin cambios; Tailwind no aplica aqui.
3. Archivo `src/i18n/locales/en.ts` — Accion `edit`. Detalle: paridad exacta de claves con `es.ts`; `social.ariaEmail: 'Send an email to César Ríos'`; `projects.items.businessSuite.description: 'Business Suite 5.0 — Public-utility suite for water, gas, power and waste, built with microservices and microfrontends. Full stack role with frontend focus.'`; `softii: 'Softii — Point-of-sale system for shops in Mexico, with a monolithic architecture. Full stack role with frontend focus.'`; `brilla: 'Brilla Portal — Management web portal for service allies, built on microservices. Full stack role with backend focus.'`; `khatarsis: 'Vue component library and environment for building interfaces with context, speed and consistency.'`; `alytos: 'Plug-and-play modular Java backend library with auto-configuration: to scaffold the base and add only business logic.'`; `about.subtitle: 'Who I am, what I have worked on and where I come from.'`; `about.body` EN espejo de ES (mismo numero de parrafos, mismos hechos). Restricciones: paridad es/en obligatoria; English-only no aplica a valores visibles (son copy i18n).
4. Decision critica logos contacto — Archivos `src/assets/brands/*` + `src/components/contact/ContactImages.vue` — Accion `read` (verificar) + `create` bloqueado pendiente. Detalle verificado: no existen `github.svg/linkedin.svg/whatsapp.svg/mail.svg`; prohibido inventar. Opcion A (recomendada para cumplir literal "4 k-image"): crear `src/assets/brands/github.svg`, `linkedin.svg`, `whatsapp.svg`, `mail.svg` locales, importar en nuevo componente como `:src`, `<k-image :src="githubLogo" alt="GitHub" fit="contain" hover="none" lazy class="..." />`; criterio documentado: offline (local, sin CDN), licencia (anotar origen MDI BSD-3 / Simple Icons CC0 en comentario English-only + cierre; preferir `fill="currentColor"` para heredar tinta y evitar hack `lightInkLogo`, o reutilizar clase `project-logo-light-ink` si fill blanco). Opcion B (descartada salvo redefinicion): `k-icon name="mdi:github/mdi:linkedin/mdi:whatsapp/mdi:email"` dentro de `<a v-effect skew>` — cero assets pero incumple "4 k-image". Executor no crea svg sin aprobacion de origen/licencia del usuario. Restricciones: no hotlinkear; no trazar a ojo; probar light/dark.
5. Archivo `src/components/contact/ContactImages.vue` — Accion `create`. Detalle: `script setup lang="ts"` English-only, `import { contact } from '@/data/portfolio'` (+ logos o data compartida del Step 1), `import { useI18n }`; template `<div class="flex flex-wrap items-center justify-center gap-4">` con 4 `<a v-effect="'skew'" :href :aria-label="t(ariaKey)" :target :rel class="flex">` cada uno con `<k-image :src :alt="label" fit="contain" hover="none" lazy class="h-16 w-16 sm:h-20 sm:w-20" />` (tamano via clase escala Tailwind, sin `width/height` props, sin `[...]`); cuarto link `:href="'mailto:' + contact.email"` sin `target/rel`; cada skew en su propio `<a>`, nunca en `k-image` ni en padre con `data-reveal`. Restricciones: cero margenes (flex+gap); English-only handlers si los hay; copy via `t()`.
6. Archivo `src/components/contact/ContactSection.vue` — Accion `edit`. Detalle: reemplazar `import SocialLinks` por `import ContactImages`; reemplazar `<social-links />` por `<contact-images />` dentro del mismo contenedor `div.flex.flex-1.flex-wrap.items-center.justify-center.gap-4[data-reveal]`; no mover `data-reveal` al link con skew; no anadir margenes ni `[...]` ni `width/height` fijos. Restricciones: Tailwind cero-margenes; skew aislado.
7. Archivo `src/components/common/SocialLinks.vue` — Accion `edit` minimo. Detalle (decision desacoplo recomendado): hero conserva `k-button relief hover` con `:href/:aria-label/:icon` + label; sustituir array local `links` por import desde `src/data/portfolio.ts` (`socialLinks`) para cumplir RULES 0.6 sin duplicar; no convertir a imagenes; no anadir skew ni `mailto:` aqui. Alternativa parametrizada `variant` se descarta por mezclar presentaciones. Restricciones: English-only; un componente principal; no margenes nuevos.
8. Archivo `src/components/projects/ProjectCard.vue` — Accion `edit`. Detalle: reemplazar bloque `<img v-if :src :alt :class="{ 'project-logo-light-ink': ... }" class="max-h-full max-w-full object-contain" loading="lazy" />` por `<k-image v-if="props.project.image" :src="props.project.image" :alt="props.project.name" fit="contain" hover="none" lazy :class="{ 'project-logo-light-ink': props.project.lightInkLogo }" class="max-h-full max-w-full" />`; mantener `div.flex.h-48` padre y wrappers `div[v-effect=skew] > k-card[v-effect=spotlight]` intactos; no pasar `width/height/rounded/bordered`; fallback iniciales intacto. Restricciones: skew/spotlight nunca comparten transform; sin `width/height` con flex; sin `[...]`.
9. Archivo `src/components/about/AboutSection.vue` — Accion `edit`. Detalle: reemplazar `<img :src="portrait" :alt="t('about.photoAlt')" class="h-auto w-60 rounded-2xl object-cover" height/width loading="lazy" />` por `<k-image :src="portrait" :alt="t('about.photoAlt')" fit="cover" hover="none" lazy class="w-60 overflow-hidden rounded-2xl" />`; no usar prop `rounded` (evita cambiar a `rounded-lg`); no pasar `width/height` props; mantener `div.flex.flex-col.gap-6[data-reveal]` y resto intacto. Restricciones: Tailwind-first; cero margenes; English-only comentarios.
10. Archivo `src/components/layout/Navbar.vue` — Accion `edit`. Detalle: reemplazar `<img :src="logo" alt="" class="h-8 w-auto" />` por `<k-image :src="logo" alt="" fit="contain" hover="none" :lazy="false" class="h-8 w-auto" />`; mantener `button[aria-label=t('nav.ariaHome')]` + `handleLogoClick`; `:lazy="false"` por logo above-fold (eager). Restricciones: `alt=""` decorativo intencional; sin `width/height` props.
11. Archivo `src/components/layout/FooterSection.vue` — Accion `edit`. Detalle: identico a Navbar: `<k-image :src="logo" alt="" fit="contain" hover="none" :lazy="false" class="h-8 w-auto" />` dentro de `button[aria-label]` + `handleLogoClick`; no introducir `backdrop-filter` ni tocar `gradual-blur`. Restricciones: mismas que Navbar.
12. Archivo `src/components/hero/HeroSection.vue` — Accion `read` (verificar, sin edit). Detalle: confirmar que sigue usando `<social-links />` (botones) + `k-button` knowMe + GSAP `data-hero`; no aplicar `k-image`/skew aqui; si Step 1 cambio la fuente data, actualizar solo el import sin tocar template/animacion.

### Verificacion

- Busqueda codigo: sin `<img` en `src/components/**`, `src/views/**`, `src/App.vue` (permitido solo dentro de `khatarsis` en `node_modules`/`../khatarsis` y en `k-image` interno); ejecutar via busqueda (`grep "<img"`), no via build.
- Revision manual ES: contacto muestra 4 imagenes con skew solo (sin scale/spotlight), `alt` GitHub/LinkedIn/WhatsApp/Email, `aria-label` via `social.aria*` + `ariaEmail`, cuarto abre `mailto:cesarandresriosvalen@gmail.com`; proyectos en orden Business Suite, Softii, Brilla, Khatarsis, Alytos con minis CV; skills Frontend incluye Vite, Herramientas `Git, IA`; about subtitulo `Quien soy, en que he trabajado y de donde vengo` + body reescrito.
- Revision manual EN: paridad de claves, subtitulo `Who I am, what I have worked on and where I come from.`, descripciones y about traducidos, `mailto:` identico.
- Visual: `k-image` renderiza portrait/logos/project media sin regresion reveal/GSAP; skew contacto no deforma layout; light/dark legible (logos + `lightInkLogo` donde aplique); sin margenes nuevos ni `[...]` ni `width/height` con flex.
- `bun run build` pendiente por RULES 0.4 (requiere permiso explicito del usuario); consignar como pendiente en cierre.
- Validacion estandares: DESIGN.md (capas data/locales/componentes respetadas, sin endpoints nuevos), CODING_STANDARDS.md (English-only code, flex+gap, sin fijos con flex, sin arbitrarios, Tailwind-first), RULES.md (sin Git/borrados, alcance acotado, scripts bloqueados, cero duplicacion con data compartida, skills subordinadas), skills (sin skills registradas en `.agents/skills/README.md`; baseline Vue 3 + Tailwind + Vite + TS + `bun` de `.agents/AGENTS.md`).

## Cierre (memoria persistente)

- Que cambio: migracion total `<img>` -> `k-image` (6 usos, 0 `<img` en `src`); nuevo `ContactImages.vue` con 4 `k-image` solo-skew (GitHub/LinkedIn/WhatsApp/mailto:) + 4 SVG locales (`github/linkedin/whatsapp` Simple Icons CC0, `mail` MDI); `projects` reordenado businessSuite, softii, brilla, khatarsis, alytos con minis del CV (ES+EN); `Vite` a frontend, `tools: ['Git','IA']`; `about.subtitle` nuevo + `about.body` reescrito ES+EN (3 parrafos); hero conserva `k-button` con data compartida (`socialLinks`). Archivos: creados 5, editados 8 (ver `### Steps`). Ajuste posterior (plan cleanup): tinta blanca dual-theme (`text-white` + `invert`).
- Verificacion: busqueda `<img` en `src` = 0; `k-image` = 6; lectura orquestador de `ContactImages.vue`, `portfolio.ts`, `es.ts`, `SocialLinks.vue`; `bun run build` (`vue-tsc -b && vite build`) VERDE ejecutado con permiso en el plan `portfolio-contact-white-predeploy-cleanup` (2026-09-12), `dist/` emitido; `lint:check` y `format:check` verdes.
- Resultado: aprobado por el usuario ("vale perfecto", 2026-09-12).
- Pendientes: ninguno (la revision visual light/dark vive en el plan cleanup como pendiente del usuario).
